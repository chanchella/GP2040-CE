name: OAG Universal Core V1

on:
  push:
    branches:
      - chatgpt/oag-universal-core-v1
    paths:
      - 'oag/**'
      - 'docs/oag-universal-core-v1/**'
      - '.github/workflows/oag-universal-core-v1.yml'
      - '.gitmodules'
      - 'lib/pico_pio_usb'
      - 'lib/tinyusb'
  workflow_dispatch:

jobs:
  core-policy-and-tests:
    runs-on: ubuntu-22.04

    steps:
      - name: Checkout ChatGPT OAG branch
        uses: actions/checkout@v4.1.1
        with:
          fetch-depth: 0

      - name: Verify branch ownership
        shell: bash
        run: |
          set -euo pipefail
          test "${GITHUB_REF_NAME}" = "chatgpt/oag-universal-core-v1"

      - name: Verify U10K three-root attach recovery scope
        shell: bash
        run: |
          set -euo pipefail
          U10J_BASELINE="278da5b28020f1c335dfba21e644407ad7c1df4b"
          U10F_BASELINE="7799d270d98ae09e7608f05b15f687a20a459871"

          git merge-base --is-ancestor "${U10J_BASELINE}" HEAD
          git merge-base --is-ancestor "${U10F_BASELINE}" HEAD

          RUNTIME_CHANGED="$(git diff --name-only "${U10J_BASELINE}" -- \
            oag/firmware/include \
            oag/firmware/src \
            oag/firmware/tusb_config.h \
            oag/include \
            oag/src | sort)"

          test "${RUNTIME_CHANGED}" = "oag/firmware/src/main.cpp"

          # Pairing Mode, Bluetooth parser/transport, receiver, Player-1/KM,
          # Hub/XInput/HID drivers and the three-root backend are frozen.
          git diff --exit-code "${U10J_BASELINE}" -- \
            oag/firmware/include/oag/firmware/bluetooth_host_v2.h \
            oag/firmware/src/bluetooth_host_v2.cpp \
            oag/firmware/include/oag/firmware/bluetooth_hid_parser_v2.h \
            oag/firmware/src/bluetooth_hid_parser_v2.cpp \
            oag/firmware/include/oag/firmware/usb_pio_host.h \
            oag/firmware/src/usb_pio_host.cpp \
            oag/firmware/src/usb_pio_probe.c \
            oag/firmware/src/xinput_host.cpp \
            oag/firmware/src/usb_descriptors.cpp \
            oag/firmware/tusb_config.h \
            oag/firmware/include/oag/firmware/pc_xinput_device.h \
            oag/firmware/src/pc_xinput_device.cpp \
            oag/firmware/include/oag/firmware/pc_xinput_platform_driver.h \
            oag/firmware/src/pc_xinput_platform_driver.cpp \
            oag/include/oag/input/gamepad_state.h \
            oag/src/mapping/keyboard_mouse_gamepad_mapper.cpp

          grep -F 'serviceMissingRootAttachRecovery();' oag/firmware/src/main.cpp
          grep -F 'usbHost_.reconcileRootEvents(' oag/firmware/src/main.cpp
          grep -F '0,' oag/firmware/src/main.cpp
          grep -F 'kMissingRootStableUs = 2000000ull' oag/firmware/src/main.cpp
          grep -F 'kRootAttachRetryUs = 5000000ull' oag/firmware/src/main.cpp

          # Fail closed if the new service ever starts synthesizing REMOVE.
          python3 - <<'PY'
          from pathlib import Path

          text = Path("oag/firmware/src/main.cpp").read_text()
          start = text.index("void serviceMissingRootAttachRecovery()")
          end = text.index("void serviceKeyboardLeds()", start)
          body = text[start:end]

          if "plan.removeMask" in body:
              raise SystemExit("U10K must never synthesize root REMOVE")

          if "reconcileRootEvents(\n                0,\n                attachMask" not in body:
              raise SystemExit("U10K attach-only reconciliation contract missing")

          print("U10K_ATTACH_ONLY_ROOT_RECOVERY=PASS")
          PY

          echo "U10K_U10J_PAIRING_RECEIVER_ROUTING_FREEZE=PASS"
          echo "U10K_USB_HUB_AND_HOST_BACKEND_FREEZE=PASS"

      - name: Verify no Antigravity coupling
        shell: bash
        run: |
          set -euo pipefail
          ! grep -R "antigravity/g2e3-work" oag

      - name: Architecture dependency guard
        shell: bash
        run: |
          set -euo pipefail
          python3 oag/tools/check_architecture.py

      - name: Configure host tests
        shell: bash
        run: |
          set -euo pipefail
          cmake -S oag -B build/oag-host \
            -DCMAKE_BUILD_TYPE=Release \
            -DOAG_BUILD_HOST_TESTS=ON

      - name: Build host tests
        shell: bash
        run: |
          set -euo pipefail
          cmake --build build/oag-host --parallel 2

      - name: Run host tests
        shell: bash
        run: |
          set -euo pipefail
          ctest --test-dir build/oag-host --output-on-failure

  firmware-u10k:
    needs: core-policy-and-tests
    runs-on: ubuntu-22.04

    steps:
      - name: Checkout ChatGPT OAG branch with submodules
        uses: actions/checkout@v4.1.1
        with:
          fetch-depth: 0
          submodules: recursive

      - name: Setup CMake
        uses: jwlawson/actions-setup-cmake@v2
        with:
          cmake-version: "3.24.x"

      - name: Setup Pico SDK toolchain
        uses: Fortinbra/RaspberryPiPicoBuild@v7

      - name: Checkout exact Arduino-Pico 6.1.1 SDK generation
        uses: actions/checkout@v4.1.1
        with:
          repository: raspberrypi/pico-sdk
          ref: 98a542c1a62fb549ffb5d66a3e5892b06276b670
          path: pico-sdk-u9d
          submodules: recursive
          fetch-depth: 1

      - name: Verify exact U10K SDK and Bluetooth dependencies
        shell: bash
        run: |
          set -euo pipefail
          test "$(git -C pico-sdk-u9d rev-parse HEAD)" = "98a542c1a62fb549ffb5d66a3e5892b06276b670"
          test "$(git -C pico-sdk-u9d/lib/btstack rev-parse HEAD)" = "075a0780f0fad7ff67d58ac19f46e8953656a752"
          test "$(git -C pico-sdk-u9d/lib/cyw43-driver rev-parse HEAD)" = "055d64274b014dd7b1c2fc94d26e8a18face7124"
          echo "PICO_SDK_PATH=${GITHUB_WORKSPACE}/pico-sdk-u9d" >> "${GITHUB_ENV}"
          echo "U10K_PICO_SDK_230=PASS"
          echo "U10K_BTSTACK_HIDS_HOST=PASS"

      - name: Verify and prepare pinned dependencies
        shell: bash
        run: |
          set -euo pipefail
          python3 oag/tools/prepare_dependencies.py

      - name: Configure Pico 2 W U10K firmware
        shell: bash
        run: |
          set -euo pipefail
          PICO_SDK_PATH="${GITHUB_WORKSPACE}/pico-sdk-u9d" \
          cmake -S oag/firmware -B build/oag-fw \
            -DCMAKE_BUILD_TYPE=Release \
            -DPICO_BOARD=pico2_w \
            -DPICO_PLATFORM=rp2350-arm-s

      - name: Build Pico 2 W U10K firmware
        shell: bash
        run: |
          set -euo pipefail
          cmake --build build/oag-fw --parallel 2

      - name: Discover and verify firmware outputs
        shell: bash
        run: |
          set -euo pipefail

          echo "Top-level firmware outputs:"
          find build/oag-fw -maxdepth 1 -type f -printf '%f\n' | sort

          mapfile -t UF2_FILES < <(find build/oag-fw -maxdepth 1 -type f -name '*.uf2' -print)
          mapfile -t ELF_FILES < <(find build/oag-fw -maxdepth 1 -type f -name '*.elf' -print)
          mapfile -t MAP_FILES < <(find build/oag-fw -maxdepth 1 -type f -name '*.map' -print)

          test "${#UF2_FILES[@]}" -eq 1
          test "${#ELF_FILES[@]}" -eq 1
          test "${#MAP_FILES[@]}" -eq 1

          echo "OAG_UF2_PATH=${UF2_FILES[0]}" >> "${GITHUB_ENV}"
          echo "OAG_ELF_PATH=${ELF_FILES[0]}" >> "${GITHUB_ENV}"
          echo "OAG_MAP_PATH=${MAP_FILES[0]}" >> "${GITHUB_ENV}"

      - name: Generate size report
        shell: bash
        run: |
          set -euo pipefail
          mkdir -p artifacts
          arm-none-eabi-size "${OAG_ELF_PATH}" | tee artifacts/size-report.txt

      - name: Generate and verify build manifest
        shell: bash
        run: |
          set -euo pipefail
          python3 oag/tools/generate_build_manifest.py \
            build/oag-fw \
            artifacts/build-manifest.json

          python3 - <<'PY'
          import json
          from pathlib import Path

          manifest = json.loads(Path("artifacts/build-manifest.json").read_text())
          assert manifest["phase"] == "U10K"
          assert manifest["features"]["usb_hub_host"] is True
          assert manifest["features"]["wired_xgip_input"] is True
          assert manifest["features"]["xgip_host_init_sequence"] is True
          assert manifest["features"]["generic_hid_structural_fallback"] is True
          assert manifest["features"]["known_usb_controller_classifier"] is True
          assert manifest["features"]["usb_host_device_capacity"] == 12
          assert manifest["features"]["usb_host_hub_capacity"] == 4
          assert manifest["features"]["logical_gamepad_slots"] == 8
          assert manifest["features"]["host_enumeration_buffer_bytes"] == 1024
          assert manifest["features"]["pc_generic_hid_output"] is False
          assert manifest["features"]["pc_xinput_output"] is True
          assert manifest["features"]["pc_xinput_output_slots"] == 4
          assert manifest["features"]["multi_xinput_output"] is True
          assert manifest["features"]["slot0_keyboard_mouse_overlay"] is False
          assert manifest["features"]["host_player1_keyboard_mouse_overlay"] is True
          assert manifest["features"]["slot_scoped_rumble_reverse"] is True
          assert manifest["features"]["rumble_reverse_runtime"] is True
          assert manifest["features"]["host_root_watchdog_2s"] is False
          assert manifest["features"]["golden_usb_host_clock_120mhz"] is True
          assert manifest["features"]["golden_xusb_continuous_rearm"] is True
          assert manifest["features"]["keyboard_boot_host"] is True
          assert manifest["features"]["keyboard_lock_led_output"] is True
          assert manifest["features"]["keyboard_default_numlock_led"] is True
          assert manifest["features"]["g808_skip_set_idle_golden_quirk"] is True
          assert manifest["features"]["mouse_boot_host"] is True
          assert manifest["features"]["generic_hid_gamepad_host"] is True
          assert manifest["features"]["platform_profile_registry"] is True
          assert manifest["features"]["platform_output_runtime_abstraction"] is True
          assert manifest["features"]["keyboard_mouse_runtime_gamepad"] is True
          assert manifest["features"]["bluetooth"] is True
          assert manifest["features"]["bluetooth_host_v2"] is True
          assert manifest["features"]["bluetooth_host_only"] is True
          assert manifest["features"]["bluetooth_peripheral_output"] is False
          assert manifest["features"]["bluetooth_max_simultaneous_peers"] == 4
          assert manifest["features"]["bluetooth_ble_hid_host"] is True
          assert manifest["features"]["bluetooth_classic_hid_host"] is True
          assert manifest["features"]["bluetooth_gamepad_input"] is True
          assert manifest["features"]["bluetooth_keyboard_input"] is True
          assert manifest["features"]["bluetooth_mouse_input"] is True
          assert manifest["features"]["bluetooth_g2e3_transport_sequence"] is True
          assert manifest["features"]["bluetooth_init_after_usb_host_ms"] == 100
          assert manifest["features"]["bluetooth_historical_hidmaster_compatibility"] is True
          assert manifest["features"]["bluetooth_local_gap_att_server"] is True
          assert manifest["features"]["bluetooth_scan_uuid_1812_only"] is True
          assert manifest["features"]["bluetooth_scan_type"] == "passive"
          assert manifest["features"]["bluetooth_scan_interval"] == 75
          assert manifest["features"]["bluetooth_scan_window"] == 50
          assert manifest["features"]["bluetooth_deferred_connect_outside_adv_callback"] is True
          assert manifest["features"]["bluetooth_u9c_bond_reset_version"] == 2
          assert manifest["features"]["bluetooth_u9d_exact_stack_generation"] is True
          assert manifest["features"]["bluetooth_hids_api"] == "hids_host"
          assert manifest["features"]["bluetooth_sdk_23_migration"] is True
          assert manifest["features"]["bluetooth_u9d_bond_reset_version"] == 3
          assert manifest["features"]["bluetooth_u9e_live_hid_report_dispatch_fix"] is True
          assert manifest["features"]["bluetooth_hids_host_accepts_gattservice_packet_type"] is True
          assert manifest["features"]["bluetooth_u9f_baseline_freeze"] is True
          assert manifest["features"]["u9f_functional_source_frozen_to_u9e"] is True
          assert manifest["features"]["bluetooth_u9g_consumer_home_0223_guide"] is True
          assert manifest["features"]["bluetooth_u9g_xbox_ble_rumble_report_id"] == 3
          assert manifest["features"]["bluetooth_u9g_xbox_ble_rumble_retry_ms"] == 50
          assert manifest["features"]["bluetooth_u9g_family_scoped_feedback"] is True
          assert manifest["features"]["bluetooth_u10a_continuous_discovery_guard"] is True
          assert manifest["features"]["bluetooth_u10a_bt_first_pc_output_routing"] is True
          assert manifest["features"]["bluetooth_u10a_primary_bt_gamepad_sticky"] is True
          assert manifest["features"]["bluetooth_u10a_max_peers"] == 4
          assert manifest["features"]["u10c_u10a_pc_output_descriptor_restored"] is True
          assert manifest["features"]["u10c_primary_select_hold_ms"] == 3000
          assert manifest["features"]["u10c_primary_select_start_share_back_fallback"] is True
          assert manifest["features"]["u10c_xbox_ble_consumer_record_share"] is True
          assert manifest["features"]["u10c_keyboard_mouse_follow_primary"] is True
          assert manifest["features"]["u10c_usb_hub_gamepads_not_port_bound"] is True
          assert manifest["features"]["u10d_independent_xinput_interfaces"] is True
          assert manifest["features"]["u10d_custom_descriptor_type"] == "0x22"
          assert manifest["features"]["u10d_sequential_endpoint_pairs"] == ["81/01", "82/02", "83/03", "84/04"]
          assert manifest["features"]["u10d_fresh_windows_serial_namespace"] == "OAG-IND"
          assert manifest["features"]["u10d_u10c_runtime_routing_frozen"] is True
          assert manifest["features"]["u10e_receiver_vid"] == "0x045E"
          assert manifest["features"]["u10e_receiver_pid"] == "0x0719"
          assert manifest["features"]["u10e_true_receiver_interfaces"] == 8
          assert manifest["features"]["u10e_gamepad_protocol"] == "0x81"
          assert manifest["features"]["u10e_aux_protocol"] == "0x82"
          assert manifest["features"]["u10e_gamepad_endpoint_pairs"] == ["81/01", "83/03", "85/05", "87/07"]
          assert manifest["features"]["u10e_aux_endpoint_pairs"] == ["82/02", "84/04", "86/06", "88/08"]
          assert manifest["features"]["u10e_presence_heartbeat_ms"] == 1000
          assert manifest["features"]["u10e_wireless_input_packet_size"] == 29
          assert manifest["features"]["u10e_receiver_rumble_decode"] is True
          assert manifest["features"]["u10e_receiver_battery_reply"] is True
          assert manifest["features"]["u10e_unique_vendor_serial_reply"] is True
          assert manifest["features"]["u10e_u10c_primary_keyboard_mouse_routing_preserved"] is True
          assert manifest["features"]["u10f_xusb22_led_player_assignment_tracking"] is True
          assert manifest["features"]["u10f_host_player1_primary_routing"] is True
          assert manifest["features"]["u10f_keyboard_mouse_follow_host_player1"] is True
          assert manifest["features"]["u10f_primary_chord_guide_fallback"] is True
          assert manifest["features"]["u10f_receiver_persona_frozen_from_u10e"] is True
          assert manifest["features"]["u10i_rebased_directly_on_u10f_tree"] is True
          assert manifest["features"]["u10i_pairing_assist_window_ms"] == 30000
          assert manifest["features"]["u10i_pairing_assist_active_ble_scan"] is True
          assert manifest["features"]["u10i_pairing_assist_no_proactive_bond_delete"] is True
          assert manifest["features"]["u10i_btstack_pin_or_key_missing_repair_only"] is True
          assert manifest["features"]["u10i_wired_attach_detach_pairing_assist"] is True
          assert manifest["features"]["u10i_existing_bt_peers_untouched"] is True
          assert manifest["features"]["u10i_receiver_primary_km_frozen_from_u10f"] is True
          assert manifest["features"]["u10j_pairing_mode_preserved_from_u10i"] is True
          assert manifest["features"]["u10j_pairing_assist_on_usb_detach_only"] is True
          assert manifest["features"]["u10j_pairing_assist_scan_type"] == "passive"
          assert manifest["features"]["u10j_pairing_assist_ble_only_window_ms"] == 30000
          assert manifest["features"]["u10j_u10f_usb_host_runtime_frozen"] is True
          assert manifest["features"]["u10j_u10f_keyboard_mouse_runtime_frozen"] is True
          assert manifest["features"]["u10j_wired_gamepad_mount_does_not_touch_bt_discovery"] is True
          assert manifest["features"]["u10k_missing_root_attach_recovery"] is True
          assert manifest["features"]["u10k_root_probe_period_ms"] == 250
          assert manifest["features"]["u10k_missing_root_stable_ms"] == 2000
          assert manifest["features"]["u10k_root_attach_retry_ms"] == 5000
          assert manifest["features"]["u10k_synthetic_root_remove_events"] is False
          assert manifest["features"]["u10k_root_dp_pins"] == [2, 4, 6]
          assert manifest["features"]["u10k_usb_hub_support_frozen"] is True
          assert manifest["features"]["u10k_pairing_mode_frozen_from_u10j"] is True
          assert manifest["features"]["u10k_bluetooth_max_peers_frozen"] == 4
          assert manifest["features"]["u10k_bluetooth_primary_player1_frozen"] is True
          assert manifest["features"]["u10k_keyboard_mouse_follow_primary_frozen"] is True
          assert manifest["verification"]["hardware_verified"] is False
          assert manifest["verification"]["hardware_verified_baseline_phase"] == "U9G"
          assert manifest["verification"]["hardware_verified_baseline_sha"] == "3fffa9484f159df6cfbeb807c3acbdf46bdbe52e"
          assert manifest["verification"]["u9f_simultaneous_wired_bluetooth_regression"] == "HARDWARE_VERIFIED"
          assert manifest["verification"]["u9g_xbox_ble_guide_rumble"] == "HARDWARE_VERIFIED"
          assert manifest["verification"]["u10a_single_bluetooth_primary_plus_wired_gamepad_keyboard_mouse"] == "HARDWARE_VERIFIED"
          assert manifest["verification"]["u10a_multibluetooth_2_to_4_peers"] == "PENDING_HARDWARE"
          assert manifest["verification"]["u10b_receiver_descriptor_experiment"] == "REJECTED_RETURNED_TO_U10A"
          assert manifest["verification"]["u10c_primary_selection_chord"] == "HARDWARE_VERIFIED"
          assert manifest["verification"]["u10d_independent_controller_output"] == "REJECTED_HARDWARE"
          assert manifest["verification"]["u10e_true_xbox360_wireless_receiver"] == "PARTIAL_HARDWARE_VERIFIED_PRIMARY_NUMBERING_MISMATCH"
          assert manifest["verification"]["u10f_host_player1_primary_alignment"] == "HARDWARE_VERIFIED_BY_USER"
          assert manifest["verification"]["u10g_bluetooth_recovery"] == "REJECTED_LINK_ONLY_NO_HIDS"
          assert manifest["verification"]["u10h_bluetooth_recovery"] == "REJECTED_REBASED_TO_U10F"
          assert manifest["verification"]["u10i_pairing_mode_recovery"] == "HARDWARE_VERIFIED_BY_USER"
          assert manifest["verification"]["u10i_wired_pairing_assist"] == "NOT_SEPARATELY_VERIFIED"
          assert manifest["verification"]["u10i_u10f_regression"] == "FAILED_USB_HOST_PORTS_AND_GAMEPADS"
          assert manifest["verification"]["u10j_pairing_mode_preservation"] == "HARDWARE_VERIFIED_BY_USER"
          assert manifest["verification"]["u10j_three_usb_root_ports"] == "FAILED_PORT3"
          assert manifest["verification"]["u10j_wired_gamepads"] == "PARTIAL_HARDWARE"
          assert manifest["verification"]["u10j_keyboard_mouse_all_ports"] == "PARTIAL_P1_P2_ONLY"
          assert manifest["verification"]["u10k_pairing_mode_preservation"] == "PENDING_HARDWARE"
          assert manifest["verification"]["u10k_three_usb_root_ports"] == "PENDING_HARDWARE"
          assert manifest["verification"]["u10k_usb_hub"] == "PENDING_HARDWARE"
          assert manifest["verification"]["u10k_four_bluetooth_peers"] == "PENDING_HARDWARE"
          assert manifest["verification"]["u10k_primary_and_keyboard_mouse_routing"] == "PENDING_HARDWARE"
          assert manifest["dependencies"]["pico_sdk"] == "98a542c1a62fb549ffb5d66a3e5892b06276b670"
          assert manifest["dependencies"]["btstack"] == "075a0780f0fad7ff67d58ac19f46e8953656a752"
          assert manifest["dependencies"]["cyw43_driver"] == "055d64274b014dd7b1c2fc94d26e8a18face7124"
          assert manifest["features"]["mouse_aim_recenter_us"] == 6000
          assert manifest["features"]["default_platform_profile"] == "PC_XINPUT_360"
          assert manifest["pc_xinput_profile"]["compatibility_pid"] == "0x0719"
          assert manifest["pc_xinput_profile"]["console_authentication"] is False
          assert manifest["dependencies"]["pico_pio_usb"] == "5a37a66dc5d3fbe0ef3cdbeda923a757440f984f"
          assert manifest["dependencies"]["tinyusb"] == "9865cba11ecbcdd25237ba9cf4ccbe3fd1fd821d"
          assert manifest["verification"]["ci_verified"] is True
          print("OAG_MANIFEST_VERIFY=PASS")
          PY

      - name: Package exact-SHA artifacts
        shell: bash
        run: |
          set -euo pipefail
          SHORT_SHA="${GITHUB_SHA::8}"

          cp "${OAG_UF2_PATH}" \
            "artifacts/OAG_Abo_Gemi_Ultra_Gaming_U10K_${SHORT_SHA}_Pico2W.uf2"

          cp "${OAG_ELF_PATH}" \
            "artifacts/OAG_Abo_Gemi_Ultra_Gaming_U10K_${SHORT_SHA}_Pico2W.elf"

          cp "${OAG_MAP_PATH}" \
            "artifacts/OAG_Abo_Gemi_Ultra_Gaming_U10K_${SHORT_SHA}_Pico2W.map"

      - name: Upload U10K firmware bundle
        uses: actions/upload-artifact@v4.3.1
        with:
          name: OAG-Abo-Gemi-Ultra-Gaming-U10K-${{ github.sha }}
          path: artifacts/*
          if-no-files-found: error
